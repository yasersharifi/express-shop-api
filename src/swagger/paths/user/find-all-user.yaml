---
get:
    tags:
        - user
    summary: Find All Users
    description: ''
    produces:
        - application/json
    security: 
      - Bearer: []
    responses:
      200:
        description: successful operation for find all users
        schema:
          type: object
          properties:
            status: 
              type: number
              required: true
              enum: [200]
            statusText: 
              type: string
              required: true
              enum: ['success']
            data:
              $ref: '#/definitions/User'
      401:
        description: UnAuthorized Authentication
        schema:
          $ref: '#/definitions/Unauthorized'
      403:
        description: Not Access This Content
        schema:
          $ref: '#/definitions/Forbidden'
          